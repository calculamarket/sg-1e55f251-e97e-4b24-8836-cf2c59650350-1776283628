import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

<![CDATA[import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient
...

        headers: { ...corsHeaders, "Content-Type": "application/json" },
      }
    );
  }
});
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 5643 chars.]

serve(handler);
